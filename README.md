# Rrattus_popgen

`ANGSD` = code associated with ANGSD-related analysis

`dispersal_inference` = code associated with the dispersal inference workflow

`Rattus_mito_index` = code used to confirm all samples are focus species (Rattus rattus).

`sequencing_processing` = code associated with raw sequencing processing and genotype hardcalling using GATK
